[mysql]
HOSTNAME="localhost"
USERNAME="sqluser"
PASSWORD="sqlpassword"
DATABASE="sqldb"

[asset]
ACCESSKEY="uuid or something"

[renderer]
DOMAIN=arl.lambda.cam
RCCGAMEIP=localhost #ip for the 2013 arbiter (needs to be rewritten anyways)
DISABLED=false # not really needed anymore 

[arbiter]
LOC=localhost     #ip of the arbiter
PUB_LOC=localhost #public ip of the arbiter used for joining